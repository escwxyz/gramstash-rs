// use teloxide_tests::{MockBot, MockMessagePhoto, MockMessageText};

#[tokio::test]
async fn test_download_media() {
    // let bot = MockBot::new();
    // let message = MockMessageText::new("https://www.instagram.com/p/CHOYQrhl7mK/");
    // let media_type = MediaType::Photo;
    // let result = handle_message(bot, message, media_type).await;
    // assert!(result.is_ok());
}
